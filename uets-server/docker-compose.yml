services:
  uets-server:
    build: .
    image: ghcr.io/nyxiereal/userfiles/uets-server:latest
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - DATA_DIR=/app/data
